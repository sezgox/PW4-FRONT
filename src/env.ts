export const environment = {
    production: false,
    apiUrl: 'http://localhost:3002',  // Reemplaza con la URL de tu API para producción
  }

export const USERS_ENDPOINT = {
    users: '/users',
    login: '/login',
    register: '/register',
    profile: '/profile/',
    auth: '/auth'
}

export const NOTES_ENDPOINT = {
    notes: '/notes',
    edit: '/edit/',
    add: '/add',
}
